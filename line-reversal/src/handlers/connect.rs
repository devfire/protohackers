use std::net::SocketAddr;

use line_reversal::{errors::LRCPError, types::Session};

pub fn handle_connect(
    session: Session,
    client_addr: &SocketAddr,
) -> anyhow::Result<(), LRCPError> {



Ok(())
}
